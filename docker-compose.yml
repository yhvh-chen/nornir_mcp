services:
  nirnor-mcp:
    build:
      context: .
      dockerfile: Dockerfile # This still points to the updated Dockerfile
    container_name: nirnor-mcp-server
    ports:
      - "8000:8000"
    volumes:
      - ./conf:/app/conf
    restart: unless-stopped