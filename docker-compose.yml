version: '3'

services:
  nornir-mcp:
    build: .
    container_name: nornir-mcp
    volumes:
      - ./conf:/app/conf
    env_file:
      - .env
    ports:
      - "8000:8000"  # Adjust if your MCP server uses a different port
    restart: unless-stopped
